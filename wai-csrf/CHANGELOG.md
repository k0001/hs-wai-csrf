# Version 0.2

* Build also with GHC 9.4

* Depend on `ram` instead of `memory`.

* Use `__Host-CSRF-TOKEN` as default cookie name.

* Remove the `reject` field from `Config`.

* Add `tokenFromRequest`.

# Version 0.1

* Initial version.
